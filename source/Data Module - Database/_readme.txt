to create the source database, either run the "database_init.sql" script
or create a database named "MushroomPP" by hand

to create the dummy source database run the "dummy_data_init.sql" script

to init the dbo schema run the following scripts:

schema_main_delete.sql		(optional; wipes the dbo schema)
schema_main_init.sql
schema_main_populate.sql
dummy_data_insert.sql